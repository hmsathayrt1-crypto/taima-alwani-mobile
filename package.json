{
  "name": "taima-alwani-mobile",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build:web": "echo 'Using pre-built web assets from dist/'",
    "cap:sync": "npx cap sync",
    "cap:open:android": "npx cap open android",
    "build:android": "npx cap sync android",
    "init:android": "npx cap add android"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
